
# Frontend

## Setup

```bash

npm install

npm run dev

```

O app ficará disponível em [http://localhost:3000](http://localhost:3000/).

Para gerar uma build de produção:

```bash

npm run build

npx serve dist

```
